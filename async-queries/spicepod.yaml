version: v1
kind: Spicepod
name: async-queries

runtime:
  scheduler:
    state_location: file:///tmp/spiceai-async-queries/scheduler-state

datasets:
  - from: s3://spiceai-public-datasets/hive_partitioned_data/
    name: data
    params:
      file_format: parquet
      s3_auth: public
